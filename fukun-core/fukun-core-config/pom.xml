<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xmlns="http://maven.apache.org/POM/4.0.0"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <groupId>com.fukun</groupId>
    <artifactId>fukun-core-config</artifactId>
    <version>${config.parent.version}</version>
    <packaging>pom</packaging>
    <name>fukun-core-config</name>
    <description>fukun-core-config</description>

    <modules>
        <module>fukun-core-config-server</module>
        <module>fukun-core-config-server01</module>
    </modules>

    <properties>
        <config.parent.version>1.0.0-SNAPSHOT</config.parent.version>
    </properties>

    <parent>
        <groupId>com.fukun</groupId>
        <artifactId>fukun-core</artifactId>
        <version>${parent.version}</version>
        <relativePath>../pom.xml</relativePath>
    </parent>

</project>